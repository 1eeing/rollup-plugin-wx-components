{
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "publish": "lerna publish"
  },
  "name": "rollup-plugin-wx-components",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
