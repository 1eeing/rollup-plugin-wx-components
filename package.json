{
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "publish": "lerna run build && lerna publish"
  },
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
